# OPS-004: Frontend Vercel deployment

**Status:** ✅ Completed
**Priority:** HIGH
**Started:** 2026-03-27
**Completed:** 2026-03-27
**PR:** N/A (manual deploy via Vercel dashboard)

## Goal

Deploy the Next.js frontend (`apps/web`) to Vercel, connected to the Railway API backend.

## Steps

- [ ] Import GitHub repo on Vercel → set Root Directory to `apps/web`
- [ ] Add env var: `NEXT_PUBLIC_API_BASE_URL` = Railway API URL (from OPS-003)
- [ ] Confirm build settings (Next.js auto-detected, no vercel.json needed)
- [ ] Trigger deploy, confirm build passes
- [ ] Note the Vercel deployment URL → update memory/infra.md

## Notes

- Only 1 env var required: `NEXT_PUBLIC_API_BASE_URL`
- `next.config.mjs` has `ignoreBuildErrors: true` and `images.unoptimized: true` — no special Vercel config needed
- Railway API URL is the blocker; can import project on Vercel now and update env var after OPS-003 completes
- Auto-deploy from `main` branch enabled by default on Vercel

## Completion Criteria

- Vercel deployment URL is live and returns the frontend
- `NEXT_PUBLIC_API_BASE_URL` points to the Railway API
- Vercel URL recorded in `memory/infra.md`
