# DEV-016: Implement PATCH /resources/{id} (update)

**Status:** ✅ Completed
**Completed:** 2026-03-17
**Priority:** High (Tier 3, priority 7)
**Started:** 2026-03-17 00:00
**Branch:** feature/dev-015-017-resource-crud
**PR:** #50

## Requirements

**Type:** Backend
**BDD Reference:** Scenario: User updates a resource title; Scenario: User updates original_content triggering reprocessing
**Description:** Update user-editable fields. If `original_content` changes, set `status=PENDING` and enqueue re-processing job.

**Acceptance Criteria:**
- [x] Only provided fields are updated (partial update / PATCH semantics)
- [x] `updated_at` is refreshed
- [x] Changing `original_content` triggers re-processing (set status=PENDING; enqueue job if queue available, else skip enqueue gracefully)
- [x] 404 if not found or not owned

**Estimated Effort:** M
**Dependencies:** DEV-006 ✅, DEV-012 ✅

## Implementation Notes

- Batch task — implemented in same branch/PR as DEV-015 and DEV-017
- DEV-019 (task queue) is not yet implemented — the re-processing trigger should set `status=PENDING` in the DB but skip the actual job enqueue (stub it with a TODO comment or a guard: `if settings.task_queue_enabled`)
- Use SQLAlchemy partial update pattern: only set fields that are present in the request body (use `model_dump(exclude_unset=True)`)
- Return the updated resource using `ResourceResponse`

## Review Rounds

1

## Progress Log

- 2026-03-17 00:00 — Dispatched to implementer (batch with DEV-015, DEV-017)
- 2026-03-17 17:57 — Implementation complete, PR #50 created
- 2026-03-17 21:59 — Review round 1 complete: APPROVED
  Feedback: PATCH semantics properly implemented via `exclude_unset=True` for partial updates. Status reset correctly triggers on original_content change detection. updated_at automatically handled via SQLAlchemy onupdate. Comprehensive test coverage including edge cases (empty body, invalid content, unauthenticated access). All CI checks passed.
