# DEV-072: Add Alembic migration: resources_search_idx (GIN functional index)

**Status:** ✅ Completed
**Priority:** HIGH
**Started:** 2026-03-28
**Branch:** feature/dev-072-gin-search-index
**PR:** #196

## Requirements

Add an Alembic migration that creates a functional GIN index on the `resources` table for Phase 1 full-text search. The index covers `title`, `summary`, and `tags` (cast to text) via a `to_tsvector` expression. No new column is required — this is a pure index migration. `CREATE INDEX CONCURRENTLY` avoids table lock in production.

### Acceptance Criteria

- [ ] Alembic migration file created: `add_resources_search_idx`
- [ ] `alembic upgrade head` runs clean
- [ ] Index `resources_search_idx` exists on the `resources` table post-migration using `GIN (to_tsvector('english', COALESCE(title,'') || ' ' || COALESCE(summary,'') || ' ' || COALESCE(tags::text,'[]')))`
- [ ] `alembic downgrade` removes the index
- [ ] Existing migration chain is unbroken

### Design spec
docs/design-search.md §4.1

## Review Rounds

1

## Progress Log

- 2026-03-28 — Dispatched to implementer
- 2026-03-28 12:16 — Migration created: c88b95c7269d_add_resources_search_idx.py
- 2026-03-28 12:17 — PR #196 created, targeting staging branch
- 2026-03-28 16:20 — Review round 1 complete: APPROVED (static review passed)
- 2026-03-28 16:20 — CI FAILURE: `asyncpg.exceptions.ActiveSQLTransactionError: CREATE INDEX CONCURRENTLY cannot run inside a transaction block`
  Fix: wrap the `op.execute()` call in `with op.get_context().autocommit_block():` (Alembic non-transactional DDL pattern)
- 2026-03-28 — Fix applied, all CI checks pass, PR #196 merged to staging ✅
