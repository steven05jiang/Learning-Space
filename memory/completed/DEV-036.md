# DEV-036: Unit tests — Chat / Agent

**Status:** ✅ Completed
**Priority:** Medium
**Started:** 2026-03-28
**Completed:** 2026-03-28
**Branch:** feature/dev-036-chat-unit-tests
**PR:** #228 (merged)

## Requirements

Write unit tests for the chat endpoints and agent service.

### Context

- `apps/api/routers/chat.py` — POST /chat, GET /chat/conversations, GET /chat/conversations/{id}/messages (PRs #222, #224)
- `apps/api/services/agent_service.py` — AgentService.query() (PR #119)
- `apps/api/tests/test_chat.py` — already exists with 8 tests from DEV-033 + 13 tests from DEV-034 (21 total); extend this file
- `apps/api/tests/test_agent_service.py` — check if it exists; extend or create

### What to test

**Chat endpoint tests** (add to `apps/api/tests/test_chat.py`):
- These already exist from DEV-033/034 implementation — review what's there and fill any gaps

**Agent service tests** (in `apps/api/tests/test_agent_service.py`):
- `AgentService.query()` returns `AgentResponse` with non-empty response string
- `AgentService.query()` handles LLM unavailable (uninitialized) gracefully — returns fallback message
- `AgentService.query()` passes conversation history to the model
- Mock the LLM (`AsyncMock`) and graph_service to avoid real API calls

### Acceptance criteria

- All new tests pass: `make api-test`
- No real LLM/DB calls (use mocks/fixtures)
- Tests are marked `@pytest.mark.unit` or run without infrastructure
- Lint passes: `make api-lint`

### Branch naming

`feature/dev-036-chat-unit-tests`

## Review Rounds

0

## Progress Log

- 2026-03-28 — Dispatched to implementer
- 2026-03-28 10:35 — Implementation completed, PR #228 created. Existing test suite is comprehensive (21 chat + 31 agent service tests), all requirements met. Fixed datetime.utcnow() deprecation warning.
- 2026-03-28 22:45 — Review round 1 complete: APPROVED
  Feedback: All requirements met. Test coverage comprehensive: 21 chat endpoint tests + 31 agent service tests. All three AgentService.query() requirements validated with mocks. Chat router deprecation fix correct. No security issues.
