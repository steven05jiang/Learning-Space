# DEV-005: Implement OAuth login flow (multi-provider)

**Status:** ✅ Complete
**Priority:** High
**Started:** 2026-03-15 00:00
**Branch:** feature/dev-005-oauth-login-flow
**PR:** #13

## Requirements

**Type:** Backend
**BDD Reference:** Scenario: User logs in with Twitter for the first time; Scenario: User logs in with an existing linked account
**Description:** Implement `GET /auth/login/{provider}`, `GET /auth/callback` for Twitter, Google, and GitHub. On callback: exchange code for tokens, fetch user info, look up `user_accounts` by `(provider, external_id)`. If not found, create `users` + `user_accounts`. If found, update `last_login_at` and tokens. Issue JWT or set session cookie.

**Acceptance Criteria:**
- [x] Login flow works for Twitter, Google, and GitHub
- [x] New users get a `users` row and `user_accounts` row created
- [x] Returning users are authenticated to their existing `users` row
- [x] JWT or session cookie is issued on successful auth
- [x] `last_login_at` is updated on each login

**Estimated Effort:** L (~1hr)
**Dependencies:** DEV-002, DEV-004 (both complete)

## Review Rounds
2

## Progress Log
- 2026-03-15 00:00 — Dispatched to implementer
- 2026-03-15 01:50 — OAuth implementation completed and PR #13 created
- 2026-03-15 02:15 — Review round 1 complete: CHANGES REQUESTED
  Feedback:
  - CRITICAL: Hardcoded JWT secret in config.py (line 21) — requires validation or secure default
  - HIGH: Missing PKCE implementation for Twitter (hardcoded state/challenge)
  - HIGH: Missing base64 encoding for HTTP Basic auth header in Twitter exchange
  - MEDIUM: Generic exception handler leaks errors to client in oauth_callback (line 99)
  - MEDIUM: No HTTPS validation on redirect_uri construction
  - Code quality: Exceeds 15-file delivery standard (19 files total)
  - Tests pass: 13/13 unit tests, linting passes, but integration test skipped
- 2026-03-15 02:30 — Review feedback round 1 fixes applied and pushed
  - Fixed all critical security issues (JWT secret validation, PKCE, Basic auth)
  - Implemented proper error handling and HTTPS validation
  - Fixed and unskipped test coverage
  - All tests passing (14/14), linting clean
- 2026-03-15 03:00 — Review round 2 complete: APPROVED
  Feedback: none
  - All 14 tests passing (including test_oauth_callback_success)
  - Linting clean (ruff passes)
  - Delivery standards met: 5 files, 99 net lines
  - JWT_SECRET_KEY validation: Required, 32+ chars, rejects defaults
  - PKCE implementation correct: proper code_verifier, SHA256 challenge, secure state
  - Basic auth encoding correct: base64(client_id:client_secret)
  - Error handling secure: logs server-side, generic message to client
  - HTTPS validation in place for production
  - No hardcoded secrets
  - OAuth flow complete for all three providers (Twitter, GitHub, Google)
- 2026-03-15 03:15 — PR #13 merged
