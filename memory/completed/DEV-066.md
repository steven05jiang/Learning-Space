---
name: DEV-066
description: Restrict login UI to Google-only — remove X/Twitter button and email/password form from LoginForm
type: task
---

# DEV-066: Restrict login UI to Google-only

**Status:** ✅ Completed
**Completed:** 2026-03-27
**Priority:** High
**Started:** 2026-03-27
**Branch:** feature/dev-066-google-only-login
**PR:** #158 (merged)

## Requirements

Remove the X/Twitter OAuth button and the entire email/password form from the login page.
Google OAuth should be the only login method shown to users.

### Key file: `apps/web/components/LoginForm.tsx`

Current state:
- Email/password form (lines ~148–206) with email input, password input, "Sign in" submit button, "Forgot password?" link
- "or continue with" divider
- Two-column grid with Google button + X button
- "Don't have an account? Sign up" link at bottom

Target state:
- Remove the entire `<form>` block (handleSignIn, email/password state, isLoading state)
- Remove the divider ("or continue with")
- Remove the X button and its handler (handleXLogin, isXLoading state)
- Remove the "Don't have an account? Sign up" link
- Make the Google button full-width (remove the 2-column grid, replace with single button)
- Keep: branding/logo header, mock mode badge, error display, handleGoogleLogin + Google button

### Also remove unused state/handlers:
- `email`, `password`, `isLoading` state
- `isXLoading` state
- `handleSignIn` function
- `handleXLogin` function
- Remove unused imports: `Input`, `Label` (if no longer used after form removal)

### Mock mode: keep working
- In mock mode, clicking Google should still navigate to /dashboard
- `isMock` conditional in `handleGoogleLogin` remains

### Tests
- Check `apps/web/__tests__/login.test.tsx` — update or remove tests that reference the removed form/X button

### Acceptance criteria
- `apps/web/components/LoginForm.tsx` shows only the Google button (full width)
- No email/password form rendered
- No X button rendered
- `make web-lint` passes
- `make web-build` passes

## Review Rounds

1 — APPROVED

## Progress Log

- 2026-03-27 — Dispatched to implementer
- 2026-03-27 — Implemented Google-only login UI, removed email/password form and X button, created PR #158
- 2026-03-27 10:15 — Review round 1 complete: APPROVED
  Feedback: All requirements met. X button and email/password form completely removed. Google button full-width. Unused imports and state cleaned up. Tests updated to match new UI. No code quality or security issues.
