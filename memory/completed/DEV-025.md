# DEV-025: Implement graph service (Neo4j operations)

**Status:** ✅ Completed
**Priority:** High (Tier 3 — most upstream graph blocker)
**Started:** 2026-03-21
**Completed:** 2026-03-21
**Branch:** feature/dev-025-graph-service
**PR:** #90 (merged)

## Requirements

**Type:** Backend
**BDD Reference:** Graph is updated after resource is processed; Graph is updated after resource deletion; Graph is updated after resource re-processing
**Description:** Create a graph service with methods:
- `update_from_resource(owner_id, tags[])` — merge Tag nodes and RELATED_TO edges; increment weights
- `remove_resource_tags(owner_id, old_tags[])` — decrement/remove edges
- `cleanup_orphan_tags(owner_id)` — remove Tag nodes with no remaining edges

**Acceptance Criteria:**
- [x] Tag nodes are created with `owner_id` scoping
- [x] `RELATED_TO` edges are created for each tag pair with weight
- [x] Weights are incremented on new resources and decremented on removal
- [x] Zero-weight edges and orphan tags are cleaned up

**Dependencies:** DEV-003 ✅ (Neo4j connection + schema setup complete)

## Implementation Summary

GraphService implemented at `apps/api/services/graph_service.py` (218 lines). Three core methods: `update_from_resource` creates Tag nodes with owner_id scoping and bidirectional RELATED_TO edges with weight tracking; `remove_resource_tags` decrements weights and removes zero-weight edges; `cleanup_orphan_tags` removes orphan Tag nodes. All Cypher queries use parameterized placeholders (no injection risk). Comprehensive test suite with 10 unit tests at `apps/api/tests/test_graph_service.py` (289 lines). All 213 unit tests pass; linting passes.

## Review Rounds

1 round before approval

## Progress Log

- 2026-03-21 — Dispatched to implementer
- 2026-03-21 — Implementation complete: GraphService with all three required methods, comprehensive test suite, PR #90 created
- 2026-03-21 — Review round 1: APPROVED — all requirements met, no security issues
- 2026-03-21 — PR #90 merged
