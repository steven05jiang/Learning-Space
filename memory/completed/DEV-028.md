# DEV-028: Implement GET /graph endpoint

**Status:** ✅ Completed
**Priority:** High
**Started:** 2026-03-21 21:15
**Branch:** feature/dev-028-get-graph
**PR:** #99

## Requirements

Implement `GET /graph` (and `GET /graph?root=TagName`) — the core knowledge graph exploration endpoint. Returns nodes and edges for the authenticated user's personal knowledge graph from Neo4j.

### Response shape (must match frontend mock contract)

```json
{
  "nodes": [
    { "id": "AI", "label": "AI", "level": "current" }
  ],
  "edges": [
    { "source": "AI", "target": "Python", "weight": 3 }
  ]
}
```

- `level` values: `"root"`, `"current"`, `"child"` (three-level display per technical design)
- `id` and `label` = tag name (node_id for graph operations)
- Without `?root=TagName`: return all tag nodes + edges for the user
- With `?root=TagName`: return the given tag as `level: "current"`, its direct neighbors as `level: "child"`, and the neighbors' neighbors as `level: "parent"`

### Implementation notes

- Add route to `apps/api/routers/graph.py`
- Query Neo4j via `graph_service` (or direct neo4j_driver session)
- Use `owner_id = current_user.id` to scope to the authenticated user
- Add Pydantic schemas for request/response to `apps/api/schemas/` (GraphNode, GraphEdge, GraphResponse)
- Register router already exists at `/graph` prefix in main app
- Must handle empty graph (no tags yet) → return `{ "nodes": [], "edges": [] }`

### Schema to add (in `apps/api/schemas/graph.py` — create if not exists)

```python
class GraphNode(BaseModel):
    id: str
    label: str
    level: str  # "root", "current", "child"

class GraphEdge(BaseModel):
    source: str
    target: str
    weight: int

class GraphResponse(BaseModel):
    nodes: list[GraphNode]
    edges: list[GraphEdge]
```

## Key files

- `apps/api/routers/graph.py` — add `GET /graph` route
- `apps/api/schemas/graph.py` — create graph schemas
- `apps/api/services/graph_service.py` — add `get_graph(owner_id, root=None)` method
- `apps/api/tests/test_graph.py` — unit tests (mock Neo4j driver)

## Acceptance Criteria

- [ ] `GET /graph` returns `{nodes, edges}` for authenticated user
- [ ] `GET /graph?root=TagName` returns scoped subgraph
- [ ] Empty graph returns `{nodes: [], edges: []}`
- [ ] Unauthenticated request returns 401
- [ ] Response schema matches mock handler contract (id, label, level / source, target, weight)
- [ ] Unit tests pass with mocked Neo4j
- [ ] `make api-lint` and `make api-test` pass

## Review Rounds

3

## Progress Log

- 2026-03-21 — Created, waiting for DEV-026 to merge first
- 2026-03-21 21:15 — Started implementation
- 2026-03-21 21:45 — Implementation complete, PR #99 created
  - Added GraphNode, GraphEdge, GraphResponse schemas in schemas/graph.py
  - Added get_graph() method to GraphService for both all-nodes and rooted subgraph queries
  - Added GET /graph route with optional ?root parameter
  - Added 5 comprehensive unit tests with mocked Neo4j driver
  - All tests pass, lint checks pass
- 2026-03-21 23:45 — Code review round 1 complete: CHANGES REQUESTED
  Feedback:
  1. CRITICAL: Rooted subgraph missing parent-level nodes (neighbors' neighbors). Requirement states three levels (current, child, parent) but implementation only returns two levels. Neo4j query and response building logic need to fetch and include parent nodes.
  2. HIGH: Test for rooted subgraph doesn't validate the three-level structure. Test only checks that nodes/edges exist but doesn't verify parent-level nodes are present.
  3. MEDIUM: Endpoint docstring incomplete — omits "parent" level requirement.
- 2026-03-21 23:55 — Review fixes implemented and pushed to PR #99
  - Fixed Cypher query to fetch three levels: root → children → parents (neighbors of neighbors)
  - Updated response building logic to include parent-level nodes with correct level labels
  - Added edge deduplication to prevent duplicate edges in response
  - Updated endpoint docstring to document all three levels
  - Enhanced test to validate three-level structure with assertions for parent nodes
  - All unit tests pass, lint checks pass
- 2026-03-21 23:59 — Code review round 2 complete: CHANGES REQUESTED
  Feedback:
  1. LOW: Schema documentation issue. GraphNode.level comment lists "root", "current", "child" but implementation also returns "parent" level for rooted subgraph queries. Update comment to include "parent" in the valid level values.
  Summary: All three critical issues from Round 1 successfully resolved. Rooted subgraph now correctly returns all three levels with comprehensive test validation and updated docstring. Implementation properly handles authentication, empty graphs, and nonexistent roots. All 5 unit tests pass, lint checks pass.
- 2026-03-21 23:59 — Code review round 3 complete: APPROVED
  Feedback: Schema comment on line 11 of `apps/api/schemas/graph.py` corrected. Valid level values now documented as "root", "current", "child", "parent". Fix is minimal and precise, resolves round 2 LOW issue. No new issues.
