# OPS-003: Backend Dockerfile + Railway deployment

**Status:** ⏳ Pending
**Feedback:** N/A
**Priority:** HIGH | **Effort:** M
**Depends on:** DEV-047, OPS-002
**Design spec:** N/A

## Description

Create a production-ready Dockerfile for the FastAPI backend (and ARQ worker). Deploy to Railway with auto-deploy from the `main` branch. Configure all environment variables. Run Alembic migrations on first deploy and confirm the `/health` endpoint responds. Note: DEV-047 (Dockerfiles) is now scoped to backend-only since Vercel handles frontend builds natively.

## Acceptance Criteria

- [ ] `apps/api/Dockerfile` created; `docker build` succeeds locally
- [ ] ARQ worker runs in the same image (separate Railway service or `CMD` override)
- [ ] Railway project created with two services: `api` and `worker`
- [ ] All required env vars configured in Railway (DATABASE_URL, NEO4J_URI, REDIS_URL, ANTHROPIC_API_KEY, GOOGLE_CLIENT_ID/SECRET, ALLOWED_EMAILS, etc.)
- [ ] Alembic migrations run on deploy (via Railway start command or release command)
- [ ] `GET /health` returns 200 on the Railway public URL
- [ ] Auto-deploy from `main` branch confirmed working
- [ ] `memory/infra.md` updated with Railway service URLs

## Branch name
(TBD)

## Current PR
(TBD)

## Progress Log
2026-03-27 — Task created from project-plan (deploy prioritization)
