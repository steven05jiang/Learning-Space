# DEV-058: Update worker pipeline to use processing_status state machine

**Status:** ⏳ Pending
**Feedback:** FB-002
**Priority:** HIGH | **Effort:** S
**Depends on:** DEV-057, DEV-023 ✅

## Description

Update the `process_resource` worker job to respect the `processing_status` field. Workers must set the status to `processing` when starting, `success` or `failed` on completion, and skip resources that are already in a terminal state (`success` or `failed`). No automatic re-processing on failure — recovery is manual (see DEV-059).

## Acceptance Criteria

- [ ] Worker sets `processing_status = processing` before LLM/fetch call begins
- [ ] Worker sets `processing_status = success` after pipeline completes successfully
- [ ] Worker sets `processing_status = failed` (with `status_message`) on any error
- [ ] Worker skips (logs and returns early) if `processing_status` is already `success` or `failed` when job starts
- [ ] Re-enqueue of an already-`success` resource is silently dropped (no duplicate processing)
- [ ] Unit tests: start→processing transition, success path, failure path, skip-if-terminal

## Branch name
(TBD)

## Current PR
(TBD)

## Progress Log
2026-03-22 — Task created from FB-002 feedback planning
