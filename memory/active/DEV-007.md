# DEV-007: Implement account linking flow

**Status:** 🔄 Active
**Priority:** High (Tier 3, Priority 1)
**Started:** 2026-03-17 00:00
**Branch:** feature/dev-007-account-linking
**PR:** #45

## Requirements

**Type:** Backend
**BDD Reference:** Scenario: User links an additional social account; Scenario: User attempts to link an account already linked to another user
**Description:** Implement `GET /auth/link/{provider}` (requires auth). Encodes `link:<user_id>` in OAuth state. On callback, if state indicates link flow, create new `user_accounts` row for current user. If `(provider, external_id)` exists for another user, return 409.

**Acceptance Criteria:**
- [x] Authenticated user can start link flow for any supported provider
- [x] New `user_accounts` row is created under the current user
- [x] 409 returned if account already belongs to another user
- [x] Tokens are stored for the linked account

**Estimated Effort:** M (~40min)
**Dependencies:** DEV-005 ✅, DEV-006 ✅

## Review Rounds
1

## Progress Log
- 2026-03-17 00:00 — Dispatched to implementer
- 2026-03-17 01:00 — Implementation completed, PR #45 created
- 2026-03-17 04:15 — Review round 1 complete: CHANGES REQUESTED
  Feedback: CRITICAL: Cross-user account linking vulnerability in OAuth callback endpoint. Link flow lacks authentication check, allowing an attacker to link their OAuth account to another user's account by reusing a valid link state. Fix: Add JWT authentication requirement to callback endpoint for link flows and verify current_user.id == link_user_id before linking.
- 2026-03-17 04:30 — Security vulnerability fixed
  - Added optional authentication dependency for callback endpoint
  - Added authentication verification for link flows (current_user.id == link_user_id)
  - Added comprehensive security tests (unauthenticated and mismatched user scenarios)
  - All tests passing (104 passed), lint clean
  - Commits: 317743e (security fix), a687c67 (lint fixes)
