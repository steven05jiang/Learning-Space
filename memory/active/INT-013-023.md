# INT-013-023: Resources API Integration Tests

**Status:** ⏳ Pending
**Priority:** High
**Started:** (pending — dispatched after INT-001-012 merges)
**Branch:** (pending)
**PR:** (pending)

## Scope

Write Layer 1 API integration tests for group: resources (CI: every PR)

- **INT-013** — Authenticated user POST /resources with a URL → 202, resource created with status PENDING or PROCESSING
- **INT-014** — Authenticated user POST /resources with text content → 202, resource created
- **INT-015** — Unauthenticated POST /resources → 401
- **INT-016** — POST /resources with `prefer_provider` hint → resource row has prefer_provider stored
- **INT-017** — GET /resources → paginated list, only own resources (not another user's)
- **INT-018** — GET /resources?tag=AI → filters by tag
- **INT-019** — GET /resources?status=READY → filters by status
- **INT-020** — GET /resources/{id} → full resource detail
- **INT-021** — PATCH /resources/{id} with new title → title updated, updated_at changes
- **INT-022** — PATCH /resources/{id} with new original_content → re-triggers PROCESSING + enqueues new job
- **INT-023** — DELETE /resources/{id} → resource removed, graph sync job enqueued

## File to Create

`apps/api/tests/integration/test_int_resources.py` — INT-013–023

## Existing Framework

- `apps/api/tests/integration/conftest.py` — `db_session`, `test_user`, `auth_headers`
- `apps/api/tests/integration/factories.py` — `make_resource(db, owner_id, **kwargs)`
- `apps/api/pyproject.toml` — marker `int_resources` already registered
- Makefile: `int-test-ci` default includes `resources`

## Key API Routes (from `apps/api/routers/resources.py`)

- POST `/resources` → 202
- GET `/resources` → paginated list (query params: tag, status, page, size)
- GET `/resources/{id}` → single resource
- PATCH `/resources/{id}` → update
- DELETE `/resources/{id}` → 204

## Review Rounds

0

## Progress Log

- 2026-03-20 — Created, waiting for INT-001-012 to merge first
