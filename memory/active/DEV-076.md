# DEV-076: Implement Search page UI (Next.js) + re-enable search nav

**Status:** 🔄 Active
**Priority:** HIGH
**Started:** 2026-03-28
**Branch:** (pending)
**PR:** (pending)

## Requirements

Build the `/search` Next.js page with debounced keyword input, result cards, tag filter dropdown, and proper blank/loading/empty states. Wire to `GET /resources/search`. Re-enable the sidebar search navigation item (DEV-067 added a "coming soon" tooltip — replace with functional navigation to `/search`).

### Acceptance Criteria

- [ ] `/search` route renders search input with 500ms debounce
- [ ] Blank state (no query entered): prompt text "Type to search your resources"
- [ ] Loading state: skeleton cards shown while fetching
- [ ] Empty state (query with no results): "No results for '{query}'" message
- [ ] Result cards show: title, summary, tags (as chips), top_level_categories, link (URL resources)
- [ ] `rank` field is NOT displayed to user
- [ ] Tag filter dropdown: lists all tags from current user's resources; filters results on selection
- [ ] Sidebar search nav item re-enabled — navigates to `/search` (remove "coming soon" tooltip added by DEV-067)
- [ ] Consistent with existing UI design system (shadcn/ui, OKLch color variables, dark/light mode)
- [ ] `next build` and `eslint` pass

### Design spec
docs/design-search.md §8.1, docs/ux-requirements.md §7.1

## Review Rounds

0

## Progress Log

- 2026-03-28 — Dispatched to implementer
