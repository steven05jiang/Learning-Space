# DEV-001: Initialize monorepo and project structure

**Status:** 🔄 Active (PR open, entering review)
**Priority:** Tier 1 — Foundation
**Started:** 2026-03-14
**Branch:** feature/dev-001-monorepo-init
**PR:** #1 (open)

## Requirements

Initialize the monorepo layout and scaffold both applications so that all subsequent tasks have a consistent project structure to build on.

## Implementation Summary

Scaffolded the full Learning Space monorepo on top of the existing repo. Added `apps/api/` (FastAPI, pyproject.toml managed by uv, modular core/routers/models/services/workers layout), `apps/web/` (Next.js 14 with TypeScript, Tailwind CSS, App Router), `docker-compose.yml` (PostgreSQL 16, Neo4j 5, Redis 7), `.gitignore`, and `README.md`. GitHub Actions CI workflow was authored locally but requires `workflow` PAT scope to push — pending token update.

## Review Rounds
1

## Progress Log
- 2026-03-14 — Dispatched to implementer
- 2026-03-14 — Code complete, PR #1 opened
- 2026-03-14 — Previously marked completed prematurely (PR not merged)
- 2026-03-14 — Re-entered review loop by PM
- 2026-03-14 — Review round 1 complete
  - pr-reviewer: APPROVED
  - security-reviewer: CHANGES REQUESTED
  - Feedback:
    1. **Hardcoded DB credentials in `apps/api/core/config.py`** — `database_url` defaults to `postgresql+asyncpg://learningspace:learningspace@localhost:5432/learningspace` and `neo4j_password` defaults to `"learningspace"`. Must remove plaintext credential defaults; use required env vars only (or safe placeholder without real-looking passwords).
    2. **Hardcoded credentials in `docker-compose.yml`** — `POSTGRES_PASSWORD: learningspace` and `NEO4J_AUTH: neo4j/learningspace` are committed in plaintext. Change to reference env vars (e.g. `${POSTGRES_PASSWORD:-changeme}`) or document via `.env.example` only.
