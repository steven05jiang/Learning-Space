---
name: DEV-068
description: Chat panel "coming soon" mode — disable input/send, inject welcome bot message
type: task
---

# DEV-068: Chat panel "coming soon" mode

**Status:** ⏳ Pending
**Priority:** High
**Started:** (pending)
**Branch:** (pending)
**PR:** (pending)

## Requirements

The chat panel should appear in a "coming soon" state for production deployment.
Users can open the panel but cannot send messages.

### Key file: `apps/web/components/chat-panel.tsx`

Target behavior (non-mock mode only):
- Initial bot message changed to: "AI Chat is coming soon! Check back later."
- Input field is disabled (`disabled` prop)
- Send button is disabled
- Optionally: add a subtle "Coming soon" badge or note below the input

Mock mode: unchanged (keep existing mock behavior so local dev/demo still works)

Implementation:
- In `initialMessages`, update the non-mock initial message content to the coming-soon text
- Add `disabled` to the `<Input>` element when not in mock mode (or always, since API doesn't exist yet)
- Add `disabled` to the Send button when not in mock mode
- Keep all existing JSX structure — just disable interactivity and update the initial message

### Acceptance criteria
- In non-mock mode, chat input and send button are disabled
- Initial bot message is the "coming soon" text
- Mock mode is unaffected (existing mock messages + full interactivity)
- `make web-lint` passes
- `make web-build` passes

## Review Rounds

0

## Progress Log

- 2026-03-27 — Task created, pending dispatch after DEV-067 merges
