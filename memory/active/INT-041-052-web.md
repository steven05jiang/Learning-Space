# INT-041-044 + INT-050-052: Frontend Integration Tests (Web)

**Status:** ⏳ Pending
**Priority:** Medium
**Started:** (pending — dispatched after INT-013-023 merges)
**Branch:** (pending)
**PR:** (pending)

## Scope

Write Layer 2 frontend integration tests (Jest + MSW, no backend needed).

### Group: web — resource UI (INT-041–044)
- **INT-041** — Resource submission form renders; user fills URL and submits; POST /resources called
- **INT-042** — Resource card shows PROCESSING status indicator while processing
- **INT-043** — Resource card shows FAILED status with actionable message
- **INT-044** — User browses resource list; items rendered from GET /resources response

### Group: web — settings UI (INT-050–052)
- **INT-050** — Settings page shows linked accounts from GET /auth/accounts
- **INT-051** — User clicks "Add account" → redirects to /auth/link/{provider}
- **INT-052** — User attempts to disconnect last account → error message shown (MSW returns 400)

## Files to Write Into

- `apps/web/__tests__/integration/int_resource_ui.test.tsx` — INT-041–044
- `apps/web/__tests__/integration/int_settings_ui.test.tsx` — INT-050–052

(Stubs already exist at these paths — replace stubs with real test implementations)

## Existing Framework

- `apps/web/__tests__/integration/setup.ts` — MSW server lifecycle
- `apps/web/__tests__/integration/handlers/resources.ts` — resource mock handlers
- `apps/web/__tests__/integration/handlers/auth.ts` — auth mock handlers

## CI Enablement

Tests run via: `make int-test-web`
Which executes: `cd apps/web && npm test -- --testPathPattern="integration" --watchAll=false`

## Review Rounds

0

## Progress Log

- 2026-03-20 — Created, waiting for INT-013-023 to merge first
