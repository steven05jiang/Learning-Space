# DEV-023: Implement process_resource job (full pipeline)

**Status:** ✅ Complete (Ready for Review)
**Priority:** High (Tier 3 — unblocks worker tests + graph integration)
**Started:** 2026-03-21
**Branch:** feature/dev-023-process-resource-job
**PR:** #92

## Requirements

**Type:** Backend
**BDD Reference:** All worker scenarios (INT-024–028)
**Description:** Orchestrate the full processing pipeline as an async worker job:
1. Set status → PROCESSING
2. Fetch content — use unauthenticated URL fetcher (DEV-020) for URL resources; use `original_content` directly for text resources. **DEV-021 (authenticated fetcher) is deferred — do not implement or require it.**
3. LLM processing (DEV-022) — extract title, summary, tags
4. Update resource in DB (status → READY, write title/summary/tags)
5. Trigger graph update (DEV-026 — will be integrated later; leave a clear hook/call site)
6. Handle errors at each step → status → FAILED with status_message

**Acceptance Criteria:**
- [ ] Pipeline runs end-to-end for URL resources (unauthenticated fetch only)
- [ ] Pipeline runs end-to-end for text resources (skip fetch step, use original_content)
- [ ] Status transitions: PENDING → PROCESSING → READY or FAILED
- [ ] LLM failure sets status=FAILED with descriptive status_message
- [ ] Fetch failure sets status=FAILED with descriptive status_message
- [ ] Graph update hook is present (can be a no-op / placeholder call for now; DEV-026 will wire it)

**Dependencies:** DEV-019 ✅, DEV-020 ✅, DEV-022 ✅ (DEV-021 deferred/skipped)

## Branch name
feature/dev-023-process-resource-job

## Review Rounds

1

## Progress Log

- 2026-03-21 — Dispatched to implementer. Note: DEV-021 deferred — unauthenticated fetch only.
- 2026-03-21 — Re-dispatched (prior session ended before implementation; DEV-025 now merged on main)
- 2026-03-21 — ✅ Implementation complete. PR #92 created with full pipeline:
  * Added status_message field to Resource model + migration
  * Implemented 6-stage process_resource pipeline (PROCESSING → fetch/LLM → READY/FAILED)
  * Comprehensive error handling with descriptive status messages
  * Graph update integration using merged GraphService (DEV-025)
  * 9 comprehensive unit tests covering all scenarios
  * All lint checks and 222 unit tests passing
- 2026-03-21 21:16 — Review round 1: CHANGES REQUESTED
  Feedback: Integration tests failing in CI (API · Integration Tests = FAILURE). Code quality and security are excellent (all 222 unit tests pass, lint pass, security scan pass). All 6 acceptance criteria implemented and tested. Request investigation of schema migration (JSONB → JSON change) or infrastructure issue before merging. Code is production-ready once CI tests pass.
- 2026-03-21 21:23 — Fix round 1: Identified and fixed JSONB→JSON migration issue. Removed erroneous column type change from migration (fa8cb7e566ef) that was changing tags from JSONB to JSON. Database will maintain original JSONB type for DEV-030 GIN index compatibility. All lint checks and 222 unit tests passing. Pushed commit 367b4ab.
- 2026-03-21 22:00 — Review round 2: CHANGES REQUESTED
  Root cause of integration test failure identified: DateTime mismatch in Resource model. Model uses `datetime.now(timezone.utc)` (timezone-aware) but database schema uses `TIMESTAMP WITHOUT TIME ZONE` (expects naive datetimes). Causes error: "can't subtract offset-naive and offset-aware datetimes" when inserting records. Migration fix was correct (tags remains JSONB for DEV-030). Fix: Change Resource model to use `datetime.utcnow` (naive) to match User/Account models and schema. All 222 unit tests pass. Security: no issues. Code quality: excellent. Once DateTime fix is applied, integration tests should pass.
- 2026-03-21 22:20 — Fix round 2: Fixed datetime compatibility issue. Changed Resource model and workers/tasks.py to use `datetime.utcnow()` (naive datetimes) instead of `datetime.now(timezone.utc)` (timezone-aware) to match database schema pattern. All 222 unit tests passing, all 33 integration tests passing. Lint checks pass. Pushed commit 1ab3fe1.
- 2026-03-21 22:35 — Review round 3: APPROVED
  Datetime fix verified and working correctly. All CI checks pass (lint, 222 unit tests, security scan, 33 integration tests). All 6 acceptance criteria met and working end-to-end for both URL and text resources. Error handling comprehensive with descriptive status messages. Graph update hook properly implemented with tag count check and graceful failure handling. Code quality excellent, security clean (no OWASP Top 10 issues). Ready to merge.
