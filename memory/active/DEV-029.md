# DEV-029: Implement POST /graph/expand

**Status:** ✅ Complete - PR Ready
**Priority:** High
**Started:** 2026-03-21 21:15
**Branch:** feature/dev-029-graph-expand
**PR:** #101

## Requirements

Implement `POST /graph/expand` — allows the frontend to drill down into a specific graph node and get its neighbors (child nodes + edges).

### Request body

```json
{ "node_id": "AI", "direction": "out" }
```

- `node_id` (required): the tag name to expand
- `direction` (optional): `"out"` (default), `"in"`, `"both"`

### Response shape (must match frontend mock contract)

```json
{
  "nodes": [
    { "id": "Python", "label": "Python", "level": "child" }
  ],
  "edges": [
    { "source": "AI", "target": "Python", "weight": 3 }
  ]
}
```

### Implementation notes

- Add route to `apps/api/routers/graph.py`
- Query Neo4j for direct neighbors of `node_id` scoped to `current_user.id`
- Return neighbors as `level: "child"` nodes + their edges to the root node
- If node not found or has no neighbors → return `{ "nodes": [], "edges": [] }`
- Reuse `GraphNode`, `GraphEdge`, `GraphResponse` schemas from DEV-028
- Add `get_neighbors(owner_id, node_id, direction)` method to `GraphService`

### Pydantic request schema

```python
class GraphExpandRequest(BaseModel):
    node_id: str
    direction: str = "out"  # "out", "in", "both"
```

## Key files

- `apps/api/routers/graph.py` — add `POST /graph/expand` route
- `apps/api/schemas/graph.py` — add `GraphExpandRequest` schema
- `apps/api/services/graph_service.py` — add `get_neighbors(owner_id, node_id, direction)` method
- `apps/api/tests/test_graph.py` — unit tests (mock Neo4j driver)

## Acceptance Criteria

- [x] `POST /graph/expand` with `{node_id}` returns neighbors as `{nodes, edges}`
- [x] Unknown node returns `{nodes: [], edges: []}`
- [x] Unauthenticated request returns 401
- [x] Response schema matches mock handler contract
- [x] Unit tests pass with mocked Neo4j
- [x] `make api-lint` and `make api-test` pass

## Review Rounds

2 ✅

## Progress Log

- 2026-03-21 — Created, waiting for DEV-028 to merge first
- 2026-03-21 21:15 — Started implementation after DEV-028 completion
- 2026-03-21 21:30 — Implementation complete, PR #101 created
  - Added get_neighbors method to GraphService with Neo4j query for direct neighbors
  - Added POST /graph/expand endpoint that takes node_id and direction parameters
  - Returns neighbors as child-level nodes with edges from source node
  - Added 3 comprehensive unit tests with mocked Neo4j driver
  - All tests pass, lint checks pass
- 2026-03-21 22:00 — Review round 1 complete: CHANGES REQUESTED
  Feedback: direction parameter is accepted but unused (marked as unused in code comment); Neo4j query always returns bidirectional results regardless of direction value; no validation of direction parameter; tests don't verify different direction values produce different results
- 2026-03-21 22:30 — Review feedback addressed and pushed
  - Implemented direction parameter in GraphService.get_neighbors() with proper Neo4j queries for "out" (->), "in" (<-), and "both" (-) directions
  - Added @field_validator to GraphExpandRequest schema for direction validation
  - Added 3 new test cases: test_expand_graph_direction_in, test_expand_graph_direction_both, test_expand_graph_invalid_direction
  - All tests pass (23/23), lint checks clean
  - Fixed query formatting to meet line length limits
- 2026-03-21 22:45 — Review round 2 complete: APPROVED
  Feedback: none
  Verified: All three round-1 issues resolved — direction parameter produces distinct Neo4j queries ("out" ->, "in" <-, "both" -); Pydantic validator rejects invalid directions with 422; tests cover all three direction values with query pattern verification (test_expand_graph_direction_in, test_expand_graph_direction_both, test_expand_graph_invalid_direction); all 248 API tests pass, lint checks pass.
