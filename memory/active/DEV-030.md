# DEV-030: Implement GET /graph/nodes/{node_id}/resources

**Status:** 🔄 Active
**Priority:** High (Tier 3)
**Started:** 2026-03-21
**Branch:** feature/dev-030-graph-node-resources
**PR:** #94

## Requirements

**Type:** Backend
**BDD Reference:** Scenario: User views resources for a graph node
**Description:** Query PostgreSQL for resources where `tags` contains the given tag name (node_id maps to a tag label) and `owner_id` matches the authenticated user. Return a list of resources.

**Acceptance Criteria:**
- [x] Returns resources matching the tag for the current user (owner_id scoped)
- [x] Uses GIN index on `tags` JSONB column for efficient query
- [x] Response includes `id`, `title`, `summary`, `original_content`, `content_type`, `status`
- [x] 404 if no matching resources (or empty list — acceptable)
- [x] 401 if unauthenticated

**Dependencies:** DEV-006 ✅, DEV-012 ✅

## Branch name
feature/dev-030-graph-node-resources

## Review Rounds

0

## Progress Log

- 2026-03-21 — Dispatched to implementer
- 2026-03-21 — Implementation completed, PR #94 created
- 2026-03-21 — Review feedback round 1 fixes completed and pushed
- 2026-03-21 — Review feedback round 2 fixes completed and pushed (added 5 missing tests)

## Review Round 1 (2026-03-21)

**Status:** CHANGES REQUESTED

**Issues Found:**
1. [HIGH] Linting Failure: Module-level import violation (line 42) — `import json` inside function body must be moved to module level
2. [HIGH] Requirements Mismatch: Response schema missing `original_content` and `content_type` fields — uses `ResourceListItem` instead of full fields specified in requirement
3. [MEDIUM] SQL Injection Risk: SQLite LIKE fallback (line 43) — `node_id` parameter not validated for LIKE wildcards
4. [MEDIUM] Pagination Issue: `limit` field hardcoded to result count instead of accepting configurable limit parameter

**Feedback:**
- Tests are comprehensive and well-structured
- GIN index usage for PostgreSQL is correct
- Owner scoping and 401 auth handling work as expected
- Code structure is clean but needs fixes for linting, requirements alignment, and pagination

## Review Round 2 (2026-03-21)

**Status:** CHANGES REQUESTED

**Issues Found:**
1. [HIGH] Missing Test Coverage: No tests for pagination (custom limit/offset) or node_id validation. Must add:
   - test_get_node_resources_custom_limit — Verify custom limit works and returns correct count
   - test_get_node_resources_pagination_offset — Verify offset parameter works correctly
   - test_get_node_resources_limit_boundary_100 — Verify limit=100 works, limit=101 fails with 422
   - test_get_node_resources_limit_boundary_1 — Verify limit=1 works, limit=0 fails with 422
   - test_get_node_resources_invalid_node_id — Verify invalid characters (e.g., "test%;DROP") return 422

**Verification:**
- ✅ Fix #1: import json at module level — CORRECT (line 3, graph.py)
- ✅ Fix #2: Response includes original_content, content_type, status — CORRECT (ResourceNodeItem schema includes all fields, used in response)
- ✅ Fix #3: node_id regex validation — CORRECT (lines 40-46, pattern: ^[\w\s-]+$, prevents LIKE injection)
- ✅ Fix #4: Pagination limit from query param — CORRECT (lines 25-26, bounds 1-100, applied at line 73, returned in response)

**Code Quality Assessment:**
- Implementation is clean and correct
- All 4 previous fixes properly applied
- No security vulnerabilities detected (OWASP Top 10)
- Tests pass (7 tests + unit tests)
- **Critical gap:** Missing tests for the two fixes that were most critical (validation & pagination)

**Fixes Applied:**
- ✅ Added test_get_node_resources_custom_limit (verify custom limit works)
- ✅ Added test_get_node_resources_pagination_offset (verify offset parameter)
- ✅ Added test_get_node_resources_limit_too_high (verify limit=101 returns 422)
- ✅ Added test_get_node_resources_limit_too_low (verify limit=0 returns 422)
- ✅ Added test_get_node_resources_invalid_node_id (verify invalid chars return 422)
- ✅ All 12 graph tests pass, CI check passes

**Next Steps:**
- Ready for final review

## Review Round 3 (2026-03-21)

**Status:** APPROVED

**Verification Summary:**
- ✅ All 5 required tests from round 2 present and passing:
  - test_get_node_resources_custom_limit (line 319) — Verifies custom limit works
  - test_get_node_resources_pagination_offset (line 357) — Verifies offset skips items
  - test_get_node_resources_limit_too_high (line 406) — Verifies limit=101 returns 422
  - test_get_node_resources_limit_too_low (line 422) — Verifies limit=0 returns 422
  - test_get_node_resources_invalid_node_id (line 438) — Verifies invalid chars return 422
- ✅ All 12 graph tests pass
- ✅ Lint passes (ruff check + format)
- ✅ CI check passes

**Code Quality Assessment:**
- ✅ Requirements fully met: tag filtering, owner scoping, GIN index, response schema, auth
- ✅ Pagination correctly enforced (limit: 1-100, offset: ≥0) via FastAPI Query validation
- ✅ node_id validation prevents SQL injection (regex: ^[\w\s-]+$)
- ✅ Router properly registered in main.py (line 16 import, line 51 include_router)
- ✅ Code structure clean, follows project standards
- ✅ All 4 round 1 fixes properly implemented and verified

**Security Review (OWASP Top 10):**
- ✅ No SQL injection: Parametrized queries + regex validation
- ✅ No auth bypass: get_current_user enforces 401
- ✅ Owner scoping: Query filters by owner_id == current_user.id
- ✅ No hardcoded secrets or sensitive data exposure
- ✅ Proper error handling with correct HTTP status codes

**Final Assessment:**
Implementation is production-ready. All requirements met, test coverage comprehensive, security sound. Ready to merge.
